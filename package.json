{
  "name": "agent-plugin-marketplace",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "sync": "bun run src/index.ts sync",
    "generate": "bun run src/index.ts generate"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^6.0.2"
  }
}
